#!/usr/bin/env -S uv run --script
# /// script
# requires-python = ">=3.11"
# dependencies = ["cryptography>=49.0.0"]
# ///
"""shm — self-contained Superhuman Mail CLI."""
import sys
from pathlib import Path

# Support running directly from repo root without pip install
sys.path.insert(0, str(Path(__file__).resolve().parent))

from superhuman_mail.cli import main

sys.exit(main())
